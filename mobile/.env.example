EXPO_PUBLIC_API_URL=http://localhost:5000/api

# Development Mode - Set to 'true' to persist session across app reloads
# After logging in once, your session will auto-restore on every reload/restart
EXPO_PUBLIC_DEV_MODE=false
